import * as React from "react"
import { NotFound } from '../components';
import type { HeadFC } from "gatsby"

export default NotFound;

export const Head: HeadFC = () => <title>Page introuvable</title>
